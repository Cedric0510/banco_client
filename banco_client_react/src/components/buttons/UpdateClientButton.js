import Button from 'react-bootstrap/Button';

function UpdateClientButton() {
    return (
        <>
            <Button variant="primary" href="/update-client">Mettre à jour</Button>{' '}
        </>
    )
}

export default UpdateClientButton;